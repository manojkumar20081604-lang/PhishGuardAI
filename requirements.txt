# Pinned to the versions the code was actually developed & tested against
# (verify locally with: pip install -r requirements.txt && python test_ml_features.py)
Flask==3.1.3
flask-cors==6.0.5
scikit-learn==1.9.0
nltk==3.10.3
numpy==2.5.2
pandas==3.0.5
joblib==1.5.3
python-dotenv==1.2.3
reportlab==5.0.0
werkzeug==3.1.8

# Deploy servers only (Procfile / render.yaml) - not needed for local dev
gunicorn>=21.2
